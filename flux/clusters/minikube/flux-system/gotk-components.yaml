# This file would be generated by flux bootstrap with image automation
# Run: flux install \
#   --components-extra=image-reflector-controller,image-automation-controller \
#   --export > flux/clusters/minikube/flux-system/gotk-components.yaml
# Or bootstrap with:
# flux bootstrap github \
#   --owner=<your-github-username> \
#   --repository=flux-demo \
#   --branch=main \
#   --path=./flux/clusters/minikube \
#   --components-extra=image-reflector-controller,image-automation-controller \
#   --personal

# Placeholder for Flux system components
# The actual file would contain CRDs, deployments, and services for:
# - source-controller
# - kustomize-controller
# - helm-controller
# - notification-controller
# - image-automation-controller (monitors and updates images in Git)
# - image-reflector-controller (scans container registries)